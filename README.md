# prompt-architect-v2

There, can you please create, uh, prompts folder in the root of the repo, uh, zero one prompts folder. Inside this, uh, how you will have is the, uh, general prompts. So inside this folder will be zero one general, and then afterwards you will have the project names, let's say zero two, whatever the project name is. And inside this you, you will always have the file as a sequence, I mean, uh, zero one, zero two, hyphen, the prompt slug. So whatever the s-prompt I give you, you create the slug or title for that and put it into that prompt folder specifically and make a commit. And update your .lovable, uh, folder with this memory, uh, in a MD file. And make sure you also create a what to read, uh, MD file that would actually resolve this. And also root of the repo, you create the, uh, root README file, which all the README files should be in lowercase. Remember that hyphen and lowercase, that's the format. And also in the root of the repo, you explain the folder structure, how the files needs to be, and also empty folders you cr-create with .git file so that it exists. And, um, make sure that any AI who reads it, the, the root README file, they will know what the memory structure, w-which file it needs to read and where the prompt is, how the prompt is. And every time you put the prompt, you just do a proofread, uh, nothing much. Um, so you also mention this in terms of the memory, what it needs to do in terms of if a prompt is given with the proofread prompt or next prompt. So usually the project name is given and in the prompt. If no project name, then, then it would be a general prompt. Okay? Uh, inside the general prompt, we can have other folders like, um, let's say bug fix or things like that. So every one of this folder can be zero one, zero two sequence. Uh, it could be bug fix, insults, uh, dry code, uh, coding, uh, guideline, things like that. So all kinds of folder can be there. So also you can have .git folder. So based on the category, you put that, uh, prompt in there. And if it is mentioned for a r-I mean, project, then you name the project name as this folder, as I mentioned, like, uh, whatever the sequence is, you hyphen the name of the project in lowercase. And then inside this you have zero one, zero two sequence and the prompts. Okay? Also, whatever I'm saying, write it into the memory so that any AI who reads it, they understand everything about the project structure and everything. So any time I give you the prompt, you just remove the filler words. You don't do anything else. Keep the exact words exactly as it is. So you put it into there. Um, and then you put the action item and checks item, uh, I mean, checked item that must need to follow. You create a section called Action Item Must Follow Non-Negotiable. Must, must follow. And put all these terms that has been discussed. Make sure that is, uh, written as a checklist that AI should follow. And also if there is any folder structure that is defined or discussed, that should also be in a section like folder, uh, structure, something like this. Uh, if there is a database, uh, segment or database design that has been desi-um, discussed, then also put into that, uh, end of the prompt. If, if nothing discussed regarding databases, skip that. If no folder structure discussed, skip that. Okay. Any link or any file is given, usually, uh, the idea is that that file AI should, um, write that file into, um, into the specific folder. If it is con-in confusion, it can ask. Usually the assets image, it should go into the assets folder. Uh, spec should go into the spec folder. Inside the spec folder, there should be folder twenty-one. Inside this, the app spec should be there. Um, and always the, uh, before writing the code, uh, the AI should read the, um, let's say the coding guideline, understand the coding guideline and error management must be followed. This needs to be added with every prompt. That is, uh, inside the spec folder, folder two, folder three must be followed, and zero four as well. Just mention these folder names, numbers. That would be all right. Um, so these are very, very important, uh, when writing the code. Okay. And make sure the code needs to be dry. If it is dealing with the code and, uh, spec every time, the spec needs to add that. Um, so there is that part. And, uh, yeah, so this is how the prompts and everything should go. You should actually write everything what I'm just saying, uh, in terms of the AI memory, so that it can read and understand any time it reads it, so that it does not make a mistake. Do you understand what I'm saying? If you have any question, just let me know.




Inside the prompts folder, these, these prompts which I have shared here, they should go as a sample prompt, uh, inside the general prompt, uh, the pro-prompt library setup. Okay? Inside the general, general folder, there should be a prompt library setup, and it should have this MD file with this slug, like prompt zero one prompt library setup.md file that contains this one what we just discussed, and also refer to this from the root README file also inside the .lavable folder what to read and so on. Do you understand what I'm saying? Can you please do that for me?

This project was built with [Lovable](https://lovable.dev).

## Build with Lovable

Continue developing this project in the [Lovable editor](https://lovable.dev/projects/dd4ee203-8bbd-40a8-8b9d-3435b483eb70).

- **Ship faster**: describe what you want to build and Lovable handles the code.
- **Stay in sync**: every change made in Lovable is committed straight to this repository.
- **Full ownership**: this code is yours. Push to `main` on GitHub and your changes sync back into Lovable, ready for your next prompt.

## Development

Prefer working locally? You need Node.js and npm — [install with nvm](https://github.com/nvm-sh/nvm#installing-and-updating).

```sh
git clone <this-repository-url>
cd <repository-name>
npm i
npm run dev
```
